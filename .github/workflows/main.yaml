name: main
on: push
jobs:
  jobA:
    runs-on: ubuntu-latest
    steps:
      - name: param
        run: |
          file="fil1.txt"
          echo "file=$file" >> $GITHUB_ENV
          echo "filepath/$file"
          python script.py --value "$file"
          
      - name: next_Task
        run: |
           echo "$file exist"
  
